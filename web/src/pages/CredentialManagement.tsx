export default function CredentialManagement() {
  return <div>
    <h1 style={{margin:0}}>Credentials</h1>
    <div className="card mt-4"><p className="text-muted">Manage API keys and broker credentials via environment variables or the admin panel.</p></div>
  </div>
}
