export default function WebhookConfig() {
  return <div>
    <h1 style={{margin:0}}>Webhooks</h1>
    <div className="card mt-4"><p className="text-muted">Configure webhook endpoints for external trade signals and notifications.</p></div>
  </div>
}
